import { saveCategory } from "../manage-actions";import type { AdminCategory } from "@/lib/admin/products";export function CategoryForm({item}:{item?:AdminCategory}){return <main className="admin-shell"><form action={saveCategory} className="admin-card admin-form"><input type="hidden" name="id" value={item?.id}/><h1>{item?"Edit category":"New category"}</h1><label>Name<input name="name" required defaultValue={item?.name}/></label><label>Slug<input name="slug" defaultValue={item?.slug}/></label><label>Description<textarea name="description" defaultValue={item?.description??""}/></label><label>Sort order<input name="sortOrder" type="number" min="0" defaultValue={item?.sortOrder??0}/></label><label><input name="isActive" type="checkbox" defaultChecked={item?.isActive??true}/> Active</label><button>Save category</button></form></main>}
