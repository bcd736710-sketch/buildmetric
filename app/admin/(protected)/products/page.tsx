import Link from "next/link";import { categories,products } from "@/lib/admin/products";export default async function Products({searchParams}:{searchParams:Promise<{status?:string;category?:string}>}){const q=await searchParams;const [rows,cats]=await Promise.all([products(q),categories()]);return <main className="admin-shell"><section className="admin-card wide"><h1>Products</h1><p><Link href="/admin/products/new">New product</Link></p><form><select name="status" defaultValue={q.status}><option value="">All statuses</option><option value="draft">Draft</option><option value="published">Published</option><option value="archived">Archived</option></select><select name="category" defaultValue={q.category}><option value="">All categories</option>{cats.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><button>Filter</button></form><table><thead><tr><th>Product Name</th><th>Category</th><th>Status</th><th>Featured</th><th>Updated</th><th /></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.name}</td><td>{r.categoryName}</td><td>{r.status}</td><td>{r.featured?"Yes":"No"}</td><td>{new Date(r.updatedAt).toLocaleDateString()}</td><td><Link href={`/admin/products/${r.id}/edit`}>Edit</Link></td></tr>)}</tbody></table></section></main>}
